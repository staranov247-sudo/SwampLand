services:
  db:
    image: mysql:8.0
    container_name: minecraft_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_DATABASE: minecraft_db
    ports:
      - "3307:3306"